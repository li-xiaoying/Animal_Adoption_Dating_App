{% extends 'layouts/main.j2' %}
{% block content %} 
    <h2 class="page-name">The account you provide either is "Admin" or doesn't exist or the username/password incorrect, try again!</h2>
    <script type=text/javascript src="{{ url_for('static', filename='/login_form.js') }}"></script>
    <form id="login_form" action="/adopter_login" onsubmit="return validateForm()" method="post" required>
        <div class="container">
            <pre>   Email: <input type="text" name="email"><br></pre>
            <pre>Password: <input type="password" name="password"><br></pre>
            <br>
            <button type="submit">Log In</button>
            <br>
            <br>
            <pre>Need an account? <a href="/signup">Sign Up Here<br></a></pre>
        </div>
    <form>
{% endblock %}